# football-data.org free-tier API token (server-side only — never expose to the client).
# Get one at https://www.football-data.org/client/register
FOOTBALL_DATA_API_KEY=

# Optional: override the competition code (defaults to WC = FIFA World Cup).
# FOOTBALL_DATA_COMPETITION=WC
